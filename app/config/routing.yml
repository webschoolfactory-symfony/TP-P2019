homepage:
    path: /
    defaults:
        _controller: FrameworkBundle:Redirect:urlRedirect
        path: /subjects
        permanent: true

logout:
    path: /logout

app:
    resource: "@AppBundle/Controller/"
    type:     annotation
